version: 2

workflows:
  single-node:
    steps:
      -
        name: "single-node"
        command: experiment.run_single_node
        params:
          command: python main.py
          container: pytorch/pytorch
          machineType: "P5000"